type User = {
    name: string,
    age: number
};

const person: User = {
    name: 'Adam',
    age: 30,
};

let role: [number, string] = [2, 'author'];

role.push('mango')

console.log(person.name);